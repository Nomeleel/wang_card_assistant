class App {
  String id;
  String name;
  String description;
  String iconUrl;
  String packageName;
  String bundleId;

  App(this.id, this.name, this.description, this.iconUrl, this.packageName, this.bundleId);

}