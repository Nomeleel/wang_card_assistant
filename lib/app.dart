class App {
  const App(this.id, this.name, this.description, this.iconUrl,
      this.packageName, this.bundleId, this.urlScheme);

  final String id;
  final String name;
  final String description;
  final String iconUrl;
  final String packageName;
  final String bundleId;
  final String urlScheme;
}
