class App {
  String id;
  String name;
  String description;
  String iconUrl;
  String packageName;
  String bundleId;
  String urlScheme;

  App(this.id, this.name, this.description, this.iconUrl, this.packageName, this.bundleId, this.urlScheme);

}